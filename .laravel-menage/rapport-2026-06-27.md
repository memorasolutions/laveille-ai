# Rapport de ménage Laravel — la-veille-de-stef-v2

Date : 2026-06-27 (America/Toronto) · Laravel 12.54 · 51 modules nwidart · ~515 fichiers de test.
Mode : **AUDIT read-only** (zéro modification de code). Décision : auditer d'abord, refactorer ensuite par lots validés (jamais à l'aveugle sur 51 modules en prod). Signé MEMORA solutions.

## Synthèse chiffrée

| Indicateur | Valeur | Note |
|---|---|---|
| Tests | 1 rouge (`CharteGraphiqueTest` color-modes ×4 isolé) ; Unit 20/20 verts | Couverture « live » faible (beaucoup de tests Pest en attente) |
| PHPStan (Larastan) | niveau **6**, **50 erreurs / 28 fichiers** | concentré, nettoyable |
| Pint (style) | **634 fichiers** déviants (auto-corrigeables) | ⚠️ inclut `declare_strict_types` ×162 = risque runtime, pas purement cosmétique |
| Duplication | signal modéré ; **70 fichiers** appellent `Settings::get` éparpillé | phpcpd non installé |
| God-components | **Academy** domine : `CourseEditor` 2852 l., `QuestionBankManager` 1739, `CourseAssignments` 1689 | dette structurelle |
| Dé-Claude (signatures d'auteur IA) | **0** à retirer (16 mentions = contenu/feature légitimes, à GARDER) | déjà conforme règle 11 |

## Dé-Claude — RAS (vérifié, filtré)
Aucune vraie signature d'auteur IA (`Co-Authored-By: Claude`, `@author Claude`, « Generated by Claude »…). Les 16 occurrences de « Claude/Claude Code/Anthropic » sont du **contenu de veille IA légitime** (fiches produit, seeders éditoriaux, outil « générateur de prompt POUR Claude Code », couleur de marque `#cc785c`, changelog). **Un remplacement aveugle aurait corrompu le contenu** : ne jamais le faire. Le code porte déjà `@author MEMORA solutions`.

## Roadmap priorisée (lots — à valider 1 par 1, tests verts avant/après)

| Lot | Action | Impact | Effort | Risque |
|---|---|---|---|---|
| L1 | Rafraîchir le test `color-modes` (CharteGraphiqueTest) OU confirmer le dark-mode admin → baseline 100 % verte (prérequis Phase 2 du skill) | moyen | 30 min | faible |
| L2 | PHPStan : corriger les 50 erreurs (28 fichiers : nullable, méthodes indéfinies, dead-code) — vrais petits bugs | élevé | 2-4 h | faible (ciblé) |
| L3 | Pint en **commit cosmétique isolé** — MAIS revoir `declare_strict_types` (162 fichiers) à part (peut lever des TypeError runtime) ; lancer la suite après | moyen | 1-2 h | moyen (strict_types) |
| L4 | Casser les God-components Academy (`CourseEditor`, `QuestionBankManager`, `CourseAssignments`) en services/sous-composants + golden-master Livewire | très élevé | 1-2 j | élevé (à faire 1 composant à la fois, /simulation) |
| L5 | Centraliser les 70 `Settings::get` épars (façade/cache) | moyen | variable | faible |

## Garde-fous (skill /laravel-menage)
- Backup + git commit AVANT chaque lot ; tests verts avant/après ; `/simulation` E2E pour les lots structurels (L4).
- Désactiver ≠ supprimer (données conservées). Prod = proposer seulement.
- Couverture « live » faible → écrire des **tests de caractérisation (golden master)** AVANT de refactorer L4.
