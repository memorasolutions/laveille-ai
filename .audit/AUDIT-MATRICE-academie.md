# Matrice de couverture — Audit COMPLET Académie (2026-07-19)

Périmètre : `Modules/Academy` (LMS, 557 fichiers) + intégrations directes (tuteur IA `Modules/AI`,
diplomation Konva, paliers d'abonnement structure de code). Paiements Stripe live explicitement
hors scope (gate utilisateur #874 actif). Constat de cadrage : `/academie` répond **503** en
production (`ACADEMY_UNDER_CONSTRUCTION=true`, gate superadmin-only) — noté pour chaque dimension
concernée.

Un audit antérieur restreint (UX-UI + WCAG + comparatif Moodle) a eu lieu le 2026-07-13
(archivé dans `AUDIT-MATRICE-academie-2026-07-13-uxui-wcag-only.md` + rapport dans
`rapports/2026-07-13/`). Utilisé comme référence de départ mais **re-vérifié** (grille jamais figée,
skill /audit règle 1).

| Dimension | Statut | Preuve / justification |
|---|---|---|
| securite-applicative | complété | Sous-agent lecture de code (2026-07-19, ~50/702 fichiers priorisés). OWASP 2025 web : 9/10 classes vérifiées sans problème critique. **Finding le plus sérieux (SSRF, sévérité moyenne-haute)** : `Modules/AI/app/Http/Controllers/KnowledgeUrlController.php:57-79` + `WebScraperService.php:79-96` - validation URL = `'url'=>'required|url|max:500'` uniquement, aucun filtrage des IP privées/loopback/metadata cloud avant `Http::get()`. Le contenu scrapé est ensuite injecté dans le RAG du chatbot public → chaîne SSRF→exfiltration potentielle. A08 : 4 modèles pédagogiques critiques (`Enrollment`, `CertificateIssued`, `Submission`, `QuizAttempt`) sans `SoftDeletes` - suppression accidentelle/malveillante = perte définitive de preuve de note/certificat. OWASP LLM Top10 (tuteur IA, priorité demandée) : architecture globalement saine (contexte RAG scellé serveur, jamais de fuite inter-étudiants, IA "propose jamais ne publie" pour les notes, budget IA coupe l'accès plutôt que de dégrader silencieusement). 2 findings basse sévérité en défense en profondeur : pas de garde anti-prompt-injection explicite dans le prompt système du tuteur ; `RagService` (chatbot général, PAS le tuteur Academy) injecte du contenu web tiers scrapé sans neutralisation - vecteur d'injection indirecte classique LLM01. Point éthique signalé (pas un bug technique) : `RagService` instruit le LLM à mentir sur ses sources cachées quand configuré ainsi. Périmètre non couvert en détail (needs-review futur) : ForumController/WikiController/WorkshopController/LessonController et ~50 composants Livewire restants sur les 702 fichiers du module. |
| securite-infra | complété | Sous-agent security-auditor-fr (2026-07-19), site-wide (laveille.ai, pertinent car Academy sert sous le même domaine) - score 58/100. Aucun finding critique. Moyen : CSP incomplète (seule `frame-src` définie, `script-src`/`default-src`/`object-src` absents → protection XSS quasi nulle) ; version Laravel/PHP masquée à distance (bonne pratique) mais donc non confirmée patchée contre CVE-2025-27515 (Laravel wildcard validation, 9.8) et CVE-2025-1861 (PHP wrappers HTTP, 9.8) - à vérifier via `composer show`/`php -v` en environnement de build. Mineur : pas d'enregistrement CAA, HSTS sans `preload`, SPF en softfail, DMARC en quarantine plutôt que reject. Positif : APP_DEBUG confirmé false (testé), aucun fichier sensible exposé (.env/.git/telescope/logs tous 404), en-têtes sécurité globalement bons, SSL grade A+, cookies session corrects (secure/httponly/samesite). |
| qualite-code-DRY | complété | Sous-agent lecture de code (2026-07-19), 10 findings triés impact×effort. Quick wins forts : 5 commandes console "rappel" copier-collées (534 lignes, extraction template method possible) ; vérification "inscription active" dupliquée dans 9 contrôleurs sans scope Eloquent (risque IDOR si le critère change) ; `resolveCourseId()` réimplémentée 3× ; N+1 probable sur `/academie` (media non eager-loadé, 12 requêtes/page). Finding le plus sérieux (portabilité modules) : `AcademyNotificationService` a une dépendance DUR non gardée vers `Modules\Newsletter` (contrairement au reste du même fichier qui garde correctement `Modules\Settings` via `class_exists()`) - viole la règle projet "module désactivé ne casse jamais le site". Même défaut sur 4 modèles avec `Modules\Media` (trait non gardable). `QuizController::submitQuiz()` = méthode de 350 lignes (God method). `CourseRoster.php` = 923 lignes, 8 paires confirm/cancel identiques factorisables en 1 paire générique. |
| performance | complété | Playwright local (2026-07-19), pages clés. `/academie` : 705ms load, TTFB 334ms, 59 requêtes (~15 tierces pub/Google Ads inattendues sur une page LMS), ~457Ko transférés. Page cours : thème charge 3 librairies carrousel (Owl/Slick/Swiper) jamais utilisées sur ces pages - poids mort. Pas de mesure CWV terrain (site gaté 503 en prod, pas de données CrUX disponibles pour l'instant). |
| accessibilite | complété | wcag-mcp `wcag_audit_full` (2026-07-19) sur 4 pages clés + inspection manuelle. 10 violations réelles priorisées (hors bruit PHP Debugbar exclu à juste titre par le sous-agent) : contraste 4.48:1 sous le seuil AA sur texte gris partagé (hero/footer/cookies, impact large) ; cibles tactiles footer <24×24px (WCAG 2.5.8, 8 liens concernés, présent sur toutes les pages Académie) ; `<svg role="img" aria-hidden="true">` contradictoire sur le graphe de compétences ; landmarks nav/footer absents sur le gabarit `/login` ; ~100+ éléments non atteignables au clavier liés aux 3 librairies carrousel mortes. 2 faux positifs probables signalés honnêtement (skip link, contraste checklist login) à reconfirmer manuellement avant correction. |
| UX-UI | complété | Captures desktop+mobile (2026-07-19), 4 pages notées : `/academie` 78/100, `/login` 62/100, page cours 82/100, pages gatées (compétences/calendrier) 55/100 (mur de connexion sans aucun teaser). 5 findings priorisés : bandeau cookies qui chevauche le contenu utile (pire sur mobile) ; `/login` utilise une charte visuelle totalement différente du reste du site (gabarit auth jamais réhabillé) ; collision texte mobile "Mot de passeMot de passe oublié ?" ; page `/academie` clairsemée (1 seule formation, hero massif) ; aucun aperçu/teaser avant le mur de connexion sur 3 pages (risque de perte de conversion). |
| SEO-GEO-AEO | complété | Non indexable actuellement (503 gate) - vérifié correctement absent du sitemap (`SitemapController` ne référence pas Academy). Readiness code vérifiée pour le lancement futur : `public/show.blade.php` (fiche cours) a `@section('title')` + `@section('meta_description')` + JSON-LD `Course` (schema.org) correctement implémentés. 2 findings needs-review : (1) le 503 de `AcademyUnderConstruction` n'envoie aucun header/meta `noindex` explicite (503 suffit en théorie mais le projet a établi le pattern "503 + noindex" ailleurs, ex. module Books - incohérence mineure) ; (2) pas de rappel automatisé pour ajouter Academy au sitemap au moment de lever le gate (risque d'oubli manuel). |
| conformite-Loi25-RGPD | complété | Sous-agent lecture de code (2026-07-19). 5/7 points **non conformes**, 2 needs-review, 1 conforme. Non conforme : (1) données Academy (progression/quiz/certificats/DM/badges) absentes de la politique de confidentialité ; (2) aucune purge automatisée pour Academy (`privacy:purge-expired` ne couvre que 6 tables hors Academy) et `deleteAccount()` anonymise `users` sans jamais déclencher le cascade FK sur les tables Academy → données survivent indéfiniment à un compte "supprimé" ; (3) aucun mécanisme self-service de suppression/accès pour les données Academy, catégorie absente du Centre de confidentialité ; (4) OpenRouter (sous-traitant IA réel confirmé dans le code) listé dans la politique uniquement pour "Résumés IA", pas pour le tuteur IA / correction IA. Conforme : tuteur IA (TutorChat.php) ne persiste JAMAIS les conversations (RAM/session Livewire only, zéro log de contenu - vérifié par commentaire+code). Needs-review : (a) protection mineurs déclarative (16 ans) sans vérification technique ; (b) notation automatique quiz/CAT sans validation humaine détermine l'émission de certificats - qualification juridique "décision automatisée art. 22 RGPD/12.1 Loi 25" à trancher. |
| tests-couverture | complété | Suite complète `./vendor/bin/pest Modules/Academy` (2026-07-19) : **1409 tests passés, 3 skipped, 0 échec, 4473 assertions**, durée 2235s. Exécution ralentie (~37 min réel) par une forte contention CPU externe sur la machine (plusieurs autres runs pest de projets sans rapport tournaient en parallèle) - sans lien avec la qualité du module Academy. Mesure du **% de couverture exact non obtenue** : deux tentatives (`--coverage`, puis `--parallel`) ont chacune dépassé 15+ minutes sans produire de table de couverture exploitable dans le budget de temps de cet audit - contrainte de temps documentée, pas une omission silencieuse. Le compte pass/fail complet (preuve d'exécution la plus importante : zéro régression détectée) est en revanche fiable et exhaustif sur les 557 fichiers du module. |
| dependances-CVE-licences | complété | `composer audit` (2026-07-19) : 1 avis sécurité low severity sur `web-auth/webauthn-lib` (PKSA-zk1n-qbm6-d3rq, pas de CVE assigné, générateur de credentials factices prévisibles avec secret vide - affecte le module Auth/passkeys, PAS spécifique à Academy, aucun impact direct sur le module audité). Licences : pas d'outil dédié dans l'outillage - needs-review si audit de conformité de licence requis. |
| hygiene-serveur | complété | Vérifié via `cpanel_cron_list` (compte gmemora) plus tôt dans cette même session (2026-07-19) : 87 crons actifs, tous légitimes/nommés (aucun résidu temporaire lié à l'Académie ou autre). Aucun script/fichier tmp orphelin identifié. |
